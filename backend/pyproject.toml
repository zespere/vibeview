[project]
name = "konceptura-backend"
version = "0.1.0"
description = "Minimal orchestration backend for the Konceptura MVP"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116.1",
    "neo4j>=5.28.2",
    "python-multipart>=0.0.22",
    "uvicorn>=0.35.0",
]

[tool.uv]
package = false
